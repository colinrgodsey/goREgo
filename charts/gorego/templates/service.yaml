apiVersion: v1
kind: Service
metadata:
  name: {{ include "gorego.fullname" . }}
  labels:
    {{- include "gorego.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: grpc
      protocol: TCP
      name: grpc
  selector:
    {{- include "gorego.selectorLabels" . | nindent 4 }}
